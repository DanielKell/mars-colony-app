import {Job} from './job';

export interface Colonist {
            id: number;
            date: string;
            job: Job;
            age: number;
}

//This should be colonist.ts. ADD TO OTHER FILE, it used to be encounter